# Joshua Sharman — Sydney Proposal Photographer (website)

Static landing page + quiz funnel. Single self-contained `index.html`.
Leads POST to a GoHighLevel inbound webhook (CRM stays in GHL).

- Edit `index.html`, commit, and push — GitHub Pages auto-publishes.
- Hosted via GitHub Pages.
